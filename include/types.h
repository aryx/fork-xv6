
typedef unsigned int   uint;
typedef unsigned short ushort;
typedef unsigned char  uchar;

#define NULL (void*) 0

#define DEBUG(args...) cprintf(args)

#define min(a, b) ((a) < (b) ? (a) : (b))

#define false 0
#define true 1
